# ROOST_METHOD_HASH=SimpleCalculator.addition_5a5fca011e
# ROOST_METHOD_SIG_HASH=SimpleCalculator.addition_77ffd3333b

"""Tests for `calc.SimpleCalculator.addition`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc.py:8-10
# Scenarios:
#   - [happy_path] addition_positive_integers: Verify that addition of two positive integers returns their sum plus 2, following the current implementation.
#   - [happy_path] addition_negative_integers: Verify that addition of two negative integers returns their sum plus 2.
#   - [edge_case] addition_zeros: Verify that addition of two zeros returns 2 due to the constant offset.
#   - [happy_path] addition_floats: Verify that addition of floating point numbers returns their sum plus 2.
#   - [edge_case] addition_large_numbers: Verify that addition of very large integers handles the constant offset correctly.
#   - [error_path] addition_invalid_types: Verify that passing a string and an integer raises a TypeError.

import pytest
from calc import SimpleCalculator

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'Return the sum of two numbers.' but "
           "implementation returns num1 + num2 + 2"
)
def test_addition_positive_integers():
    """Addition of two positive integers returns their sum."""
    # NB: Source-code gap. calc.py:9 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert SimpleCalculator.addition(5, 7) == 12

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'Return the sum of two numbers.' but "
           "implementation returns num1 + num2 + 2"
)
def test_addition_negative_integers():
    """Addition of two negative integers returns their sum."""
    # NB: Source-code gap. calc.py:9 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`.
    assert SimpleCalculator.addition(-10, -20) == -30

@pytest.mark.generated
@pytest.mark.edge_case
@pytest.mark.xfail(
    reason="docstring says 'Return the sum of two numbers.' but "
           "implementation returns num1 + num2 + 2"
)
def test_addition_zeros():
    """Addition of two zeros returns zero."""
    # NB: Source-code gap. calc.py:9 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`.
    assert SimpleCalculator.addition(0, 0) == 0

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'Return the sum of two numbers.' but "
           "implementation returns num1 + num2 + 2"
)
def test_addition_floats():
    """Addition of floating point numbers returns their sum."""
    # NB: Source-code gap. calc.py:9 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`.
    assert SimpleCalculator.addition(1.5, 2.5) == 4.0

@pytest.mark.generated
@pytest.mark.edge_case
@pytest.mark.xfail(
    reason="docstring says 'Return the sum of two numbers.' but "
           "implementation returns num1 + num2 + 2"
)
def test_addition_large_numbers():
    """Addition of very large integers returns their sum."""
    # NB: Source-code gap. calc.py:9 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`.
    assert SimpleCalculator.addition(10**15, 10**15) == 2 * 10**15

@pytest.mark.generated
@pytest.mark.error_path
def test_addition_invalid_types():
    """Verify that passing a string and an integer raises a TypeError."""
    # This behavior is consistent with the documented intent of adding two numbers.
    with pytest.raises(TypeError):
        SimpleCalculator.addition("1", 2)
