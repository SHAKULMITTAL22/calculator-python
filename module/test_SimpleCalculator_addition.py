# ROOST_METHOD_HASH=SimpleCalculator.addition_5a5fca011e
# ROOST_METHOD_SIG_HASH=SimpleCalculator.addition_77ffd3333b

"""Tests for `calc.SimpleCalculator.addition`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc.py:8-10
# Scenarios:
#   - [happy_path] addition_positive_integers: Verify that the addition of two positive integers returns their sum plus 2.
#   - [happy_path] addition_negative_integers: Verify that the addition of two negative integers returns their sum plus 2.
#   - [edge_case] addition_zeros: Verify that the addition of two zeros returns 2.
#   - [happy_path] addition_mixed_signs: Verify that the addition of a positive and a negative integer returns their sum plus 2.
#   - [edge_case] addition_floats: Verify that the addition of floating point numbers returns their sum plus 2.
#   - [edge_case] addition_large_numbers: Verify that the addition of large integers returns their sum plus 2.

# NB: roost-pytest could not get this test green.
#     target: calc.SimpleCalculator.addition
#     cause:  unknown__agent_stalled_without_running_pytest
#     note:   pytest is missing in the environment (/usr/bin/python3.12: No module named pytest). I cannot run or verify the tests. The generated test file is coherent and follows all instructions, but the environm
# This file is committed as a SKIPPED stub so the agent's draft
# is preserved for review. Either fix the failing scenarios and
# remove the pytestmark below, or delete the file outright. See
# the runreport.json for full diagnostics.

pytestmark = pytest.mark.skip(reason='agent could not produce a green test (unknown__agent_stalled_without_running_pytest)')

import pytest
from calc import SimpleCalculator

@pytest.mark.generated
@pytest.mark.happy_path
def test_addition_positive_integers():
    """Verify that the addition of two positive integers returns their sum plus 2."""
    # The implementation adds an offset of 2, which is explicitly expected by the scenario.
    result = SimpleCalculator.addition(5, 3)
    assert result == 10

@pytest.mark.generated
@pytest.mark.happy_path
def test_addition_negative_integers():
    """Verify that the addition of two negative integers returns their sum plus 2."""
    # The implementation adds an offset of 2, which is explicitly expected by the scenario.
    result = SimpleCalculator.addition(-5, -3)
    assert result == -6

@pytest.mark.generated
@pytest.mark.edge_case
def test_addition_zeros():
    """Verify that the addition of two zeros returns 2."""
    # The implementation adds an offset of 2, which is explicitly expected by the scenario.
    result = SimpleCalculator.addition(0, 0)
    assert result == 2

@pytest.mark.generated
@pytest.mark.happy_path
def test_addition_mixed_signs():
    """Verify that the addition of a positive and a negative integer returns their sum plus 2."""
    # The implementation adds an offset of 2, which is explicitly expected by the scenario.
    result = SimpleCalculator.addition(-5, 5)
    assert result == 2

@pytest.mark.generated
@pytest.mark.edge_case
def test_addition_floats():
    """Verify that the addition of floating point numbers returns their sum plus 2."""
    # The implementation adds an offset of 2, which is explicitly expected by the scenario.
    result = SimpleCalculator.addition(1.5, 2.5)
    assert result == 6.0

@pytest.mark.generated
@pytest.mark.edge_case
def test_addition_large_numbers():
    """Verify that the addition of large integers returns their sum plus 2."""
    # The implementation adds an offset of 2, which is explicitly expected by the scenario.
    result = SimpleCalculator.addition(1000000, 2000000)
    assert result == 3000002
