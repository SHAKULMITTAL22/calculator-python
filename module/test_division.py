# ROOST_METHOD_HASH=division_641e53a5f9
# ROOST_METHOD_SIG_HASH=division_641e53a5f9

"""Tests for `calc.division`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc.py:29-33
# Scenarios:
#   - [happy_path] zero_numerator: Verify that dividing zero by a non-zero number returns 0.0.
#   - [happy_path] division_by_negative_one: Verify that dividing a number by -1 returns the negative of that number.
#   - [error_path] type_error_on_string_input: Verify that passing a string as the first argument raises a TypeError.
#   - [edge_case] infinity_numerator: Verify that dividing infinity by a finite number returns infinity.
#   - [edge_case] infinity_denominator: Verify that dividing a finite number by infinity returns 0.0.
#   - [happy_path] repeating_decimal_result: Verify that division resulting in a repeating decimal is handled correctly.

import pytest
from calc import division

@pytest.mark.generated
@pytest.mark.happy_path
def test_zero_numerator():
    """Verify that dividing zero by a non-zero number returns 0.0."""
    assert division(0, 10) == 0.0

@pytest.mark.generated
@pytest.mark.happy_path
def test_division_by_negative_one():
    """Verify that dividing a number by -1 returns the negative of that number."""
    assert division(5, -1) == -5.0

@pytest.mark.generated
@pytest.mark.error_path
def test_type_error_on_string_input():
    """Verify that passing a string as the first argument raises a TypeError."""
    with pytest.raises(TypeError):
        division("10", 2)

@pytest.mark.generated
@pytest.mark.edge_case
def test_infinity_numerator():
    """Verify that dividing infinity by a finite number returns infinity."""
    assert division(float('inf'), 2) == float('inf')

@pytest.mark.generated
@pytest.mark.edge_case
def test_infinity_denominator():
    """Verify that dividing a finite number by infinity returns 0.0."""
    assert division(10, float('inf')) == 0.0

@pytest.mark.generated
@pytest.mark.happy_path
def test_repeating_decimal_result():
    """Verify that division resulting in a repeating decimal is handled correctly."""
    assert division(1, 3) == pytest.approx(0.3333333333333333)
