# atelier:audit
# The tests cover edge cases involving negative operands, infinity, and various representations of zero. They verify that the method returns error strings for zero-like divisors and handles invalid types appropriately.
#
# Tests:
#   - negative_divisor: The test asserts that the modulus of 10 and -3 equals -2.
#   - both_negative_operands: The test asserts that the modulus of -10 and -3 equals -1.
#   - float_zero_divisor: The test asserts that a float zero divisor returns a specific error message string.
#   - none_operand: The test asserts that passing None as an operand raises a TypeError.
#   - infinity_dividend: The test asserts that calculating the modulus of infinity results in a NaN value.
#   - boolean_false_divisor: The test asserts that using False as a divisor returns a zero-division error string.
# /atelier:audit
# NB: existing tests detected for this symbol.
#
# Hand-written test(s) found (no ROOST_METHOD_HASH header):
#   - tests/atelier/unit/module/test_SimpleCalculator_modulus.py
# This file was generated ALONGSIDE those — the user's
# tests are kept untouched. The scenarios here may
# differ from what the hand-written file covers.

"""Tests for `calc.SimpleCalculator.modulus`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc.py:23-27
# Scenarios:
#   - [edge_case] negative_divisor: Verify modulus behavior with a negative divisor.
#   - [edge_case] both_negative_operands: Verify modulus behavior when both operands are negative.
#   - [edge_case] float_zero_divisor: Verify that a floating point zero is correctly caught by the zero-divisor check.
#   - [error_path] none_operand: Verify that passing None as an operand raises a TypeError.
#   - [edge_case] infinity_dividend: Verify modulus behavior when the dividend is infinity.
#   - [edge_case] boolean_false_divisor: Verify that False is treated as zero by the divisor check.

import pytest
import math
from calc import SimpleCalculator

@pytest.mark.generated
@pytest.mark.edge_case
def test_negative_divisor():
    """Verify modulus behavior with a negative divisor."""
    result = SimpleCalculator.modulus(10, -3)
    assert result == -2

@pytest.mark.generated
@pytest.mark.edge_case
def test_both_negative_operands():
    """Verify modulus behavior when both operands are negative."""
    result = SimpleCalculator.modulus(-10, -3)
    assert result == -1

@pytest.mark.generated
@pytest.mark.edge_case
def test_float_zero_divisor():
    """Verify that a floating point zero is correctly caught by the zero-divisor check."""
    result = SimpleCalculator.modulus(10, 0.0)
    assert result == 'Cannot perform modulus by zero'

@pytest.mark.generated
@pytest.mark.error_path
def test_none_operand():
    """Verify that passing None as an operand raises a TypeError."""
    with pytest.raises(TypeError):
        SimpleCalculator.modulus(10, None)

@pytest.mark.generated
@pytest.mark.edge_case
def test_infinity_dividend():
    """Verify modulus behavior when the dividend is infinity."""
    result = SimpleCalculator.modulus(float('inf'), 3)
    assert math.isnan(result)

@pytest.mark.generated
@pytest.mark.edge_case
def test_boolean_false_divisor():
    """Verify that False is treated as zero by the divisor check."""
    result = SimpleCalculator.modulus(10, False)
    assert result == 'Cannot perform modulus by zero'
