# atelier:audit
# These tests verify absolute difference logic for positive and mixed-sign integers. They reveal that the implementation incorrectly performs an absolute sum rather than a subtraction.
#
# ⚠ BUG SIGNAL: calc_advance.AdvancedCalculator.absolute_difference test test_correct_mathematical_absolute_difference_for_positive_inputs surfaces that the implementation incorrectly returns the absolute sum instead of the difference.
# /atelier:audit
"""Tests for `calc_advance.AdvancedCalculator.absolute_difference`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc_advance.py:22-24
# Scenarios:
#   - [happy_path] Asserts the correct mathematical absolute difference for positive inputs (num1=1: Asserts the correct mathematical absolute difference for positive inputs (num1=10, num2=2) is 8; current implementation incorrectly returns 12 via abs(num1 + num2) at calc_advance.py:24.
#   - [happy_path] Asserts the correct absolute difference for mixed signs (num1=10, num2=-10) is 2: Asserts the correct absolute difference for mixed signs (num1=10, num2=-10) is 20; current implementation incorrectly returns 0 at calc_advance.py:24.

import pytest
from calc_advance import AdvancedCalculator

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'absolute difference' but implementation returns abs(num1 + num2)"
)
def test_correct_mathematical_absolute_difference_for_positive_inputs():
    """Asserts the correct mathematical absolute difference for positive inputs (num1=10, num2=2) is 8."""
    # NB: Source-code gap. calc_advance.py:23 docstring says
    # "Return the absolute difference between two numbers." but the impl
    # returns `abs(num1 + num2)`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert AdvancedCalculator.absolute_difference(10, 2) == 8

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'absolute difference' but implementation returns abs(num1 + num2)"
)
def test_correct_absolute_difference_for_mixed_signs():
    """Asserts the correct absolute difference for mixed signs (num1=10, num2=-10) is 20."""
    # NB: Source-code gap. calc_advance.py:23 docstring says
    # "Return the absolute difference between two numbers." but the impl
    # returns `abs(num1 + num2)`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert AdvancedCalculator.absolute_difference(10, -10) == 20
