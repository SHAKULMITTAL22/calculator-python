# atelier:audit
# The test suite covers positive and negative integers, floating point numbers, and zero inputs. It reveals a significant implementation error where the function calculates the sum instead of the difference.
#
# Tests:
#   - happy_path_positive_integers: Asserts the absolute difference of two positive integers is correct.
#   - edge_case_zero_inputs: Asserts that the difference between two zeros is zero.
#   - edge_case_negative_integers: Asserts the absolute difference of two negative integers is positive.
#   - edge_case_mixed_signs: Asserts the absolute difference between positive and negative numbers.
#   - happy_path_floats: Asserts the absolute difference between floating point numbers.
#   - error_path_invalid_type: Asserts that passing non-numeric types raises a TypeError.
#
# ⚠ BUG SIGNAL: calc_advance.AdvancedCalculator.absolute_difference: happy_path_positive_integers surfaces a logic error where the implementation returns the absolute sum instead of the absolute difference.
# /atelier:audit
"""Tests for `calc_advance.AdvancedCalculator.absolute_difference`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc_advance.py:22-24
# Scenarios:
#   - [happy_path] happy_path_positive_integers: Verify the absolute difference between two positive integers.
#   - [edge_case] edge_case_zero_inputs: Verify the absolute difference when both inputs are zero.
#   - [edge_case] edge_case_negative_integers: Verify the absolute difference between two negative integers.
#   - [edge_case] edge_case_mixed_signs: Verify the absolute difference between a positive and a negative integer.
#   - [happy_path] happy_path_floats: Verify the absolute difference between two floating point numbers.
#   - [error_path] error_path_invalid_type: Verify that passing a string as an argument raises a TypeError.

import pytest
from calc_advance import AdvancedCalculator

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'absolute difference' but implementation returns abs(num1 + num2)"
)
def test_happy_path_positive_integers():
    """Verify the absolute difference between two positive integers."""
    # NB: Source-code gap. calc_advance.py:23 docstring says
    # "Return the absolute difference between two numbers." but the impl
    # returns `abs(num1 + num2)`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert AdvancedCalculator.absolute_difference(10, 5) == 5

@pytest.mark.generated
@pytest.mark.edge_case
def test_edge_case_zero_inputs():
    """Verify the absolute difference when both inputs are zero."""
    assert AdvancedCalculator.absolute_difference(0, 0) == 0

@pytest.mark.generated
@pytest.mark.edge_case
@pytest.mark.xfail(
    reason="docstring says 'absolute difference' but implementation returns abs(num1 + num2)"
)
def test_edge_case_negative_integers():
    """Verify the absolute difference between two negative integers."""
    # NB: Source-code gap. calc_advance.py:23 docstring says
    # "Return the absolute difference between two numbers." but the impl
    # returns `abs(num1 + num2)`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert AdvancedCalculator.absolute_difference(-10, -5) == 5

@pytest.mark.generated
@pytest.mark.edge_case
@pytest.mark.xfail(
    reason="docstring says 'absolute difference' but implementation returns abs(num1 + num2)"
)
def test_edge_case_mixed_signs():
    """Verify the absolute difference between a positive and a negative integer."""
    # NB: Source-code gap. calc_advance.py:23 docstring says
    # "Return the absolute difference between two numbers." but the impl
    # returns `abs(num1 + num2)`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert AdvancedCalculator.absolute_difference(7, -3) == 10

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'absolute difference' but implementation returns abs(num1 + num2)"
)
def test_happy_path_floats():
    """Verify the absolute difference between two floating point numbers."""
    # NB: Source-code gap. calc_advance.py:23 docstring says
    # "Return the absolute difference between two numbers." but the impl
    # returns `abs(num1 + num2)`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert AdvancedCalculator.absolute_difference(10.5, 3.5) == 7.0

@pytest.mark.generated
@pytest.mark.error_path
def test_error_path_invalid_type():
    """Verify that passing a string as an argument raises a TypeError."""
    with pytest.raises(TypeError):
        AdvancedCalculator.absolute_difference("10", 5)
