# atelier:audit
# Tests cover standard math operations, Python-specific power behaviors, and common error paths. Coverage is comprehensive for numeric inputs and basic type validation.
# /atelier:audit
"""Tests for `calc_advance.AdvancedCalculator.exponentiation`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc_advance.py:10-12
# Scenarios:
#   - [happy_path] happy_path_positive_integers: Verify that raising a positive integer to a positive integer power returns the correct result.
#   - [edge_case] edge_case_zero_to_zero: Verify that 0 raised to the power of 0 returns 1, following Python's built-in behavior.
#   - [edge_case] edge_case_negative_exponent: Verify that a positive base with a negative exponent returns the correct reciprocal power.
#   - [edge_case] edge_case_fractional_exponent: Verify that a fractional exponent (square root) returns the correct floating point result.
#   - [error_path] error_path_zero_to_negative_power: Verify that raising 0 to a negative power raises a ZeroDivisionError.
#   - [error_path] error_path_type_error_string_base: Verify that passing a string as the base raises a TypeError.

import pytest
from calc_advance import AdvancedCalculator

@pytest.mark.generated
@pytest.mark.happy_path
def test_happy_path_positive_integers():
    """Verify that raising a positive integer to a positive integer power returns the correct result."""
    result = AdvancedCalculator.exponentiation(3, 4)
    assert result == 81

@pytest.mark.generated
@pytest.mark.edge_case
def test_edge_case_zero_to_zero():
    """Verify that 0 raised to the power of 0 returns 1, following Python's built-in behavior."""
    result = AdvancedCalculator.exponentiation(0, 0)
    assert result == 1

@pytest.mark.generated
@pytest.mark.edge_case
def test_edge_case_negative_exponent():
    """Verify that a positive base with a negative exponent returns the correct reciprocal power."""
    result = AdvancedCalculator.exponentiation(5, -1)
    assert result == 0.2

@pytest.mark.generated
@pytest.mark.edge_case
def test_edge_case_fractional_exponent():
    """Verify that a fractional exponent (square root) returns the correct floating point result."""
    result = AdvancedCalculator.exponentiation(4, 0.5)
    assert result == 2.0

@pytest.mark.generated
@pytest.mark.error_path
def test_error_path_zero_to_negative_power():
    """Verify that raising 0 to a negative power raises a ZeroDivisionError."""
    with pytest.raises(ZeroDivisionError):
        AdvancedCalculator.exponentiation(0, -1)

@pytest.mark.generated
@pytest.mark.error_path
def test_error_path_type_error_string_base():
    """Verify that passing a string as the base raises a TypeError."""
    with pytest.raises(TypeError):
        AdvancedCalculator.exponentiation('a', 2)
