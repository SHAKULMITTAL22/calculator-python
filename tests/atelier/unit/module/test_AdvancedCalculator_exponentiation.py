# atelier:audit
# The test suite covers standard exponentiation, zero-power logic, and negative fractional power calculations. It also handles edge cases like zero raised to the power of zero and confirms expected error handling for zero-based negative powers.
#
# Tests:
#   - positive_integers: Asserts that a positive base raised to a positive integer exponent returns the expected mathematical result.
#   - zero_exponent: Verifies that any non-zero base raised to the power of zero returns one.
#   - negative_exponent: Checks that a positive base raised to a negative fractional power returns the correct float value.
#   - zero_base_zero_exponent: Confirms that zero raised to the power of zero returns one as per standard Python behavior.
#   - zero_base_negative_exponent: Ensures that raising a zero base to a negative power correctly triggers a ZeroDivisionError.
#   - type_error_exponent: Validates that passing a non-numeric string as an exponent raises a TypeError.
# /atelier:audit
"""Tests for `calc_advance.AdvancedCalculator.exponentiation`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc_advance.py:10-12
# Scenarios:
#   - [happy_path] positive_integers: Verify that raising a positive integer to a positive integer power returns the correct result.
#   - [happy_path] zero_exponent: Verify that any non-zero number raised to the power of 0 returns 1.
#   - [happy_path] negative_exponent: Verify that a positive number raised to a negative fractional power returns the correct float result.
#   - [edge_case] zero_base_zero_exponent: Verify that 0 raised to the power of 0 returns 1, following Python's standard behavior.
#   - [error_path] zero_base_negative_exponent: Verify that raising 0 to a negative power raises a ZeroDivisionError.
#   - [error_path] type_error_exponent: Verify that passing a string as an exponent raises a TypeError.

import pytest
from calc_advance import AdvancedCalculator

@pytest.mark.generated
@pytest.mark.happy_path
def test_positive_integers():
    """Verify that raising a positive integer to a positive integer power returns the correct result."""
    assert AdvancedCalculator.exponentiation(2, 3) == 8

@pytest.mark.generated
@pytest.mark.happy_path
def test_zero_exponent():
    """Verify that any non-zero number raised to the power of 0 returns 1."""
    assert AdvancedCalculator.exponentiation(5, 0) == 1

@pytest.mark.generated
@pytest.mark.happy_path
def test_negative_exponent():
    """Verify that a positive number raised to a negative fractional power returns the correct float result."""
    # 4 ** -0.5 = 1 / sqrt(4) = 1 / 2.0 = 0.5
    assert AdvancedCalculator.exponentiation(4, -0.5) == 0.5

@pytest.mark.generated
@pytest.mark.edge_case
def test_zero_base_zero_exponent():
    """Verify that 0 raised to the power of 0 returns 1, following Python's standard behavior."""
    assert AdvancedCalculator.exponentiation(0, 0) == 1

@pytest.mark.generated
@pytest.mark.error_path
def test_zero_base_negative_exponent():
    """Verify that raising 0 to a negative power raises a ZeroDivisionError."""
    with pytest.raises(ZeroDivisionError):
        AdvancedCalculator.exponentiation(0, -1)

@pytest.mark.generated
@pytest.mark.error_path
def test_type_error_exponent():
    """Verify that passing a string as an exponent raises a TypeError."""
    with pytest.raises(TypeError):
        AdvancedCalculator.exponentiation(2, '3')
