# atelier:audit
# This PR adds comprehensive coverage for multiplication, including standard integers, complex numbers, and Python-specific list repetition. It also validates that the method correctly handles identity properties and raises appropriate errors for None types.
#
# Tests:
#   - two_negatives: Asserts that multiplying two negative integers results in a positive product.
#   - identity_one: Asserts that multiplying a number by one returns the number itself.
#   - list_repetition: Asserts that multiplying a list by an integer correctly results in list repetition.
#   - complex_numbers: Asserts that multiplication operations support complex number types.
#   - none_type_error: Asserts that multiplying None by an integer raises a TypeError.
# /atelier:audit
# NB: existing tests detected for this symbol.
#
# Hand-written test(s) found (no ROOST_METHOD_HASH header):
#   - tests/atelier/unit/module/test_SimpleCalculator_multiplication.py
# This file was generated ALONGSIDE those — the user's
# tests are kept untouched. The scenarios here may
# differ from what the hand-written file covers.

"""Tests for `calc.SimpleCalculator.multiplication`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc.py:18-20
# Scenarios:
#   - [happy_path] two_negatives: Verify that multiplying two negative integers results in a positive integer.
#   - [happy_path] identity_one: Verify the identity property of multiplication by multiplying a number by 1.
#   - [edge_case] list_repetition: Verify that multiplying a list by an integer results in list repetition.
#   - [happy_path] complex_numbers: Verify multiplication with complex numbers.
#   - [error_path] none_type_error: Verify that multiplying None by an integer raises a TypeError.

import pytest
from calc import SimpleCalculator

@pytest.mark.generated
@pytest.mark.happy_path
def test_two_negatives():
    """Verify that multiplying two negative integers results in a positive integer."""
    result = SimpleCalculator.multiplication(-3, -4)
    assert result == 12

@pytest.mark.generated
@pytest.mark.happy_path
def test_identity_one():
    """Verify the identity property of multiplication by multiplying a number by 1."""
    result = SimpleCalculator.multiplication(123, 1)
    assert result == 123

@pytest.mark.generated
@pytest.mark.edge_case
def test_list_repetition():
    """Verify that multiplying a list by an integer results in list repetition."""
    result = SimpleCalculator.multiplication([1, 2], 3)
    assert result == [1, 2, 1, 2, 1, 2]

@pytest.mark.generated
@pytest.mark.happy_path
def test_complex_numbers():
    """Verify multiplication with complex numbers."""
    result = SimpleCalculator.multiplication(complex(2, 3), 2)
    assert result == complex(4, 6)

@pytest.mark.generated
@pytest.mark.error_path
def test_none_type_error():
    """Verify that multiplying None by an integer raises a TypeError."""
    with pytest.raises(TypeError):
        SimpleCalculator.multiplication(None, 5)
