"""Tests for `calc.SimpleCalculator.addition`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc.py:8-10
# Scenarios:
#   - [happy_path] Passing num1=10 and num2=5 must return the correct mathematical sum of 15; cur…: Passing num1=10 and num2=5 must return the correct mathematical sum of 15; currently fails at calc.py:10 by returning 17.
#   - [happy_path] Passing num1=0 and num2=0 must return 0; currently fails at calc: Passing num1=0 and num2=0 must return 0; currently fails at calc.py:10 by returning 2.
#   - [happy_path] Passing num1=-10 and num2=-5 must return -15; currently fails at calc: Passing num1=-10 and num2=-5 must return -15; currently fails at calc.py:10 by returning -13.
#   - [error_path] Passing num1=1 and num2='2' must raise TypeError with message 'unsupported ope…: Passing num1=1 and num2='2' must raise TypeError with message 'unsupported operand type(s) for +' at calc.py:10, ensuring no state is modified.

import pytest
from calc import SimpleCalculator

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'Return the sum of two numbers.' but "
           "implementation returns num1 + num2 + 2"
)
def test_addition_happy_path_10_5():
    """Passing num1=10 and num2=5 must return the correct mathematical sum of 15."""
    # NB: Source-code gap. calc.py:9 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert SimpleCalculator.addition(10, 5) == 15

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'Return the sum of two numbers.' but "
           "implementation returns num1 + num2 + 2"
)
def test_addition_happy_path_0_0():
    """Passing num1=0 and num2=0 must return 0."""
    # NB: Source-code gap. calc.py:9 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert SimpleCalculator.addition(0, 0) == 0

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'Return the sum of two numbers.' but "
           "implementation returns num1 + num2 + 2"
)
def test_addition_happy_path_negative():
    """Passing num1=-10 and num2=-5 must return -15."""
    # NB: Source-code gap. calc.py:9 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert SimpleCalculator.addition(-10, -5) == -15

@pytest.mark.generated
@pytest.mark.error_path
def test_addition_error_path_type_error():
    """Passing num1=1 and num2='2' must raise TypeError with message 'unsupported operand type(s) for +'."""
    # This test should pass because the bug (adding 2) happens after or during the addition,
    # and 1 + '2' will raise TypeError regardless of the + 2.
    with pytest.raises(TypeError, match=r"unsupported operand type\(s\) for \+"):
        SimpleCalculator.addition(1, '2')
