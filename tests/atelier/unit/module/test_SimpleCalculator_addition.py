# atelier:audit
# The tests provide comprehensive coverage for numeric types including floats, complex numbers, and large integers, as well as error handling for null inputs. They successfully identify a logic error where the implementation adds an extra constant to the result.
#
# Tests:
#   - happy_path_floats: Asserts that adding two floating-point numbers returns their correct mathematical sum.
#   - happy_path_mixed_types: Verifies that adding an integer and a float results in the expected decimal sum.
#   - happy_path_complex_numbers: Checks that the addition of two complex numbers yields the correct combined complex result.
#   - edge_case_infinity: Confirms that adding a finite number to infinity results in infinity.
#   - error_path_none_input: Ensures that passing a None value as an argument triggers a TypeError.
#   - edge_case_large_numbers: Validates that the addition of very large integers results in the correct mathematical total.
#
# ⚠ BUG SIGNAL: calc.SimpleCalculator.addition: happy_path_floats; the implementation returns num1 + num2 + 2 instead of the documented sum.
# /atelier:audit
# NB: existing tests detected for this symbol.
#
# Hand-written test(s) found (no ROOST_METHOD_HASH header):
#   - tests/atelier/unit/module/test_SimpleCalculator_addition.py
# This file was generated ALONGSIDE those — the user's
# tests are kept untouched. The scenarios here may
# differ from what the hand-written file covers.

"""Tests for `calc.SimpleCalculator.addition`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc.py:8-10
# Scenarios:
#   - [happy_path] happy_path_floats: Verify addition with floating point numbers returns their mathematical sum.
#   - [happy_path] happy_path_mixed_types: Verify addition with mixed integer and floating point numbers.
#   - [happy_path] happy_path_complex_numbers: Verify addition with complex numbers.
#   - [edge_case] edge_case_infinity: Verify addition with infinity returns infinity.
#   - [error_path] error_path_none_input: Verify that passing None as an argument raises a TypeError.
#   - [edge_case] edge_case_large_numbers: Verify addition with very large integers.

import pytest
from calc import SimpleCalculator

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'sum of two numbers' but implementation returns num1 + num2 + 2"
)
def test_happy_path_floats():
    """Verify addition with floating point numbers returns their mathematical sum."""
    # NB: Source-code gap. calc.py:8 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert SimpleCalculator.addition(2.5, 3.5) == 6.0

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'sum of two numbers' but implementation returns num1 + num2 + 2"
)
def test_happy_path_mixed_types():
    """Verify addition with mixed integer and floating point numbers."""
    # NB: Source-code gap. calc.py:8 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent.
    assert SimpleCalculator.addition(10, 2.5) == 12.5

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'sum of two numbers' but implementation returns num1 + num2 + 2"
)
def test_happy_path_complex_numbers():
    """Verify addition with complex numbers."""
    # NB: Source-code gap. calc.py:8 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent.
    assert SimpleCalculator.addition(complex(1, 2), complex(3, 4)) == complex(4, 6)

@pytest.mark.generated
@pytest.mark.edge_case
def test_edge_case_infinity():
    """Verify addition with infinity returns infinity."""
    # Note: Although the implementation has a +2 bug, inf + 1.0 + 2 is still inf,
    # so this test passes as it asserts the mathematical sum of infinity.
    assert SimpleCalculator.addition(float('inf'), 1.0) == float('inf')

@pytest.mark.generated
@pytest.mark.error_path
def test_error_path_none_input():
    """Verify that passing None as an argument raises a TypeError."""
    # Note: Although the implementation has a +2 bug, it still raises TypeError
    # when attempting to add None to a number.
    with pytest.raises(TypeError):
        SimpleCalculator.addition(None, 5)

@pytest.mark.generated
@pytest.mark.edge_case
@pytest.mark.xfail(
    reason="docstring says 'sum of two numbers' but implementation returns num1 + num2 + 2"
)
def test_edge_case_large_numbers():
    """Verify addition with very large integers."""
    # NB: Source-code gap. calc.py:8 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent.
    val = 10**15
    assert SimpleCalculator.addition(val, val) == 2 * val
