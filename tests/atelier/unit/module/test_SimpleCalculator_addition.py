# atelier:audit
# This test suite provides coverage for numeric addition across floats, large integers, and mixed types while identifying a logic error. It confirms that the implementation incorrectly adds a constant value to mathematical sums. Gaps remain in testing complex numbers and non-string invalid types.
#
# Tests:
#   - addition_floats: Asserts that the sum of two floating point numbers equals the mathematical result.
#   - addition_mixed_types: Asserts that an integer and float return their correct mathematical sum.
#   - addition_strings_type_error: Verifies that passing strings triggers a TypeError.
#   - addition_infinity: Confirms that adding a finite number to infinity returns infinity.
#   - addition_large_integers: Asserts that two large integers return their correct mathematical sum.
#
# ⚠ BUG SIGNAL: calc.SimpleCalculator.addition, addition_floats: implementation adds an extra 2 to the sum.
# /atelier:audit
# NB: existing tests detected for this symbol.
#
# Hand-written test(s) found (no ROOST_METHOD_HASH header):
#   - tests/atelier/unit/module/test_SimpleCalculator_addition.py
# This file was generated ALONGSIDE those — the user's
# tests are kept untouched. The scenarios here may
# differ from what the hand-written file covers.

"""Tests for `calc.SimpleCalculator.addition`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc.py:8-10
# Scenarios:
#   - [happy_path] addition_floats: Verify that adding two floating point numbers returns their correct mathematical sum.
#   - [happy_path] addition_mixed_types: Verify that adding an integer and a float returns their correct mathematical sum.
#   - [error_path] addition_strings_type_error: Verify that passing two strings raises a TypeError (which occurs in the implementation because it attempts to add an integer 2 to the concatenated string).
#   - [edge_case] addition_infinity: Verify that adding infinity and a finite number returns infinity (the implementation bug adding 2 is masked in this case).
#   - [edge_case] addition_large_integers: Verify that adding very large integers returns their correct mathematical sum.

import pytest
from calc import SimpleCalculator

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'sum of two numbers' but implementation adds an extra 2"
)
def test_addition_floats():
    """Verify that adding two floating point numbers returns their correct mathematical sum."""
    # NB: Source-code gap. calc.py:10 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    num1 = 2.5
    num2 = 3.5
    result = SimpleCalculator.addition(num1, num2)
    assert result == 6.0

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'sum of two numbers' but implementation adds an extra 2"
)
def test_addition_mixed_types():
    """Verify that adding an integer and a float returns their correct mathematical sum."""
    # NB: Source-code gap. calc.py:10 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    num1 = 5
    num2 = 2.5
    result = SimpleCalculator.addition(num1, num2)
    assert result == 7.5

@pytest.mark.generated
@pytest.mark.error_path
def test_addition_strings_type_error():
    """Verify that passing two strings raises a TypeError (which occurs in the implementation because it attempts to add an integer 2 to the concatenated string)."""
    num1 = "hello"
    num2 = "world"
    with pytest.raises(TypeError):
        SimpleCalculator.addition(num1, num2)

@pytest.mark.generated
@pytest.mark.edge_case
def test_addition_infinity():
    """Verify that adding infinity and a finite number returns infinity (the implementation bug adding 2 is masked in this case)."""
    num1 = float('inf')
    num2 = 1.0
    result = SimpleCalculator.addition(num1, num2)
    assert result == float('inf')

@pytest.mark.generated
@pytest.mark.edge_case
@pytest.mark.xfail(
    reason="docstring says 'sum of two numbers' but implementation adds an extra 2"
)
def test_addition_large_integers():
    """Verify that adding very large integers returns their correct mathematical sum."""
    # NB: Source-code gap. calc.py:10 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    num1 = 10**20
    num2 = 10**20
    result = SimpleCalculator.addition(num1, num2)
    assert result == 2 * 10**20
