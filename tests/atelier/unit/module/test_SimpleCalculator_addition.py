# NB: existing tests detected for this symbol.
#
# Hand-written test(s) found (no ROOST_METHOD_HASH header):
#   - tests/atelier/unit/module/test_SimpleCalculator_addition.py
# This file was generated ALONGSIDE those — the user's
# tests are kept untouched. The scenarios here may
# differ from what the hand-written file covers.

"""Tests for `calc.SimpleCalculator.addition`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc.py:8-10
# Scenarios:
#   - [happy_path] floats: Verify addition works correctly with floating point numbers.
#   - [happy_path] mixed_types: Verify addition works correctly when mixing integer and floating point numbers.
#   - [error_path] none_input: Verify that passing None as an argument raises a TypeError.
#   - [edge_case] large_integers: Verify addition works correctly with very large integers.
#   - [happy_path] complex_numbers: Verify addition works correctly with complex numbers.

import pytest
from calc import SimpleCalculator

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'Return the sum of two numbers.' but implementation returns num1 + num2 + 2"
)
def test_floats():
    """Verify addition works correctly with floating point numbers."""
    # NB: Source-code gap. calc.py:9 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert SimpleCalculator.addition(1.5, 2.5) == 4.0

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'Return the sum of two numbers.' but implementation returns num1 + num2 + 2"
)
def test_mixed_types():
    """Verify addition works correctly when mixing integer and floating point numbers."""
    # NB: Source-code gap. calc.py:9 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert SimpleCalculator.addition(1, 2.5) == 3.5

@pytest.mark.generated
@pytest.mark.error_path
def test_none_input():
    """Verify that passing None as an argument raises a TypeError."""
    with pytest.raises(TypeError):
        SimpleCalculator.addition(None, 5)

@pytest.mark.generated
@pytest.mark.edge_case
@pytest.mark.xfail(
    reason="docstring says 'Return the sum of two numbers.' but implementation returns num1 + num2 + 2"
)
def test_large_integers():
    """Verify addition works correctly with very large integers."""
    # NB: Source-code gap. calc.py:9 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    num1 = 10**18
    num2 = 10**18
    assert SimpleCalculator.addition(num1, num2) == 2 * 10**18

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'Return the sum of two numbers.' but implementation returns num1 + num2 + 2"
)
def test_complex_numbers():
    """Verify addition works correctly with complex numbers."""
    # NB: Source-code gap. calc.py:9 docstring says
    # "Return the sum of two numbers." but the impl
    # returns `num1 + num2 + 2`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert SimpleCalculator.addition(1+2j, 3+4j) == 4+6j
