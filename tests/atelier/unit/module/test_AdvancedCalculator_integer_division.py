# atelier:audit
# This suite validates standard integer division and division by zero. It confirms that the function correctly implements floor division behavior for negative results.
# /atelier:audit
"""Tests for `calc_advance.AdvancedCalculator.integer_division`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc_advance.py:15-19
# Scenarios:
#   - [happy_path] Happy path with positive integers: num1=20, num2=4 returns 5 at calc_advance: Happy path with positive integers: num1=20, num2=4 returns 5 at calc_advance.py:19.
#   - [happy_path] Zero divisor handling: num1=10, num2=0 returns 'Cannot perform integer division : Zero divisor handling: num1=10, num2=0 returns 'Cannot perform integer division by zero' at calc_advance.py:18.
#   - [happy_path] Floor division behavior with negative numbers: num1=-7, num2=3 returns -3 (the f: Floor division behavior with negative numbers: num1=-7, num2=3 returns -3 (the floor of -2.33) at calc_advance.py:19.

import pytest
from calc_advance import AdvancedCalculator

@pytest.mark.generated
@pytest.mark.happy_path
def test_happy_path_with_positive_integers():
    """Happy path with positive integers: num1=20, num2=4 returns 5."""
    # Scenario: Happy path with positive integers: num1=20, num2=4 returns 5 at calc_advance:
    # Happy path with positive integers: num1=20, num2=4 returns 5 at calc_advance.py:19.
    assert AdvancedCalculator.integer_division(20, 4) == 5

@pytest.mark.generated
@pytest.mark.happy_path
def test_zero_divisor_handling():
    """Zero divisor handling: num1=10, num2=0 returns 'Cannot perform integer division by zero'."""
    # Scenario: Zero divisor handling: num1=10, num2=0 returns 'Cannot perform integer division by zero' at calc_advance.py:18.
    assert AdvancedCalculator.integer_division(10, 0) == 'Cannot perform integer division by zero'

@pytest.mark.generated
@pytest.mark.happy_path
def test_floor_division_behavior_with_negative_numbers():
    """Floor division behavior with negative numbers: num1=-7, num2=3 returns -3."""
    # Scenario: Floor division behavior with negative numbers: num1=-7, num2=3 returns -3 (the floor of -2.33) at calc_advance.py:19.
    assert AdvancedCalculator.integer_division(-7, 3) == -3
