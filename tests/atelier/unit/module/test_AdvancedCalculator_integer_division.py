# atelier:audit
# This PR provides comprehensive coverage for floor division across integer and float types. It confirms proper handling of division by zero and invalid input types.
#
# Tests:
#   - happy_path_positive_ints: Asserts exact integer division for positive numbers.
#   - happy_path_floor_division: Asserts division with remainder truncates toward negative infinity.
#   - happy_path_float_inputs: Asserts float inputs result in a floor-divided float quotient.
#   - edge_case_zero_divisor: Asserts a specific error message is returned when dividing by zero.
#   - edge_case_negative_numbers: Asserts floor division behavior for negative numerators.
#   - error_path_type_error: Asserts that strings trigger a TypeError.
# /atelier:audit
"""Tests for `calc_advance.AdvancedCalculator.integer_division`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc_advance.py:15-19
# Scenarios:
#   - [happy_path] happy_path_positive_ints: Verify that integer division of two positive integers with an exact result returns the correct integer quotient.
#   - [happy_path] happy_path_floor_division: Verify that integer division of two positive integers with a remainder correctly performs floor division (truncates towards negative infinity).
#   - [happy_path] happy_path_float_inputs: Verify that the method handles floating point inputs by performing floor division and returning a float result.
#   - [edge_case] edge_case_zero_divisor: Verify that dividing by zero returns the specific error message string instead of raising a ZeroDivisionError.
#   - [edge_case] edge_case_negative_numbers: Verify floor division behavior with negative numbers (e.g., -11 // 4 is -2.75 which floors to -3).
#   - [error_path] error_path_type_error: Verify that passing a non-numeric type (string) as an argument results in a TypeError.

import pytest
from calc_advance import AdvancedCalculator

@pytest.mark.generated
@pytest.mark.happy_path
def test_happy_path_positive_ints():
    """Verify that integer division of two positive integers with an exact result returns the correct integer quotient."""
    assert AdvancedCalculator.integer_division(20, 5) == 4

@pytest.mark.generated
@pytest.mark.happy_path
def test_happy_path_floor_division():
    """Verify that integer division of two positive integers with a remainder correctly performs floor division (truncates towards negative infinity)."""
    assert AdvancedCalculator.integer_division(10, 3) == 3

@pytest.mark.generated
@pytest.mark.happy_path
def test_happy_path_float_inputs():
    """Verify that the method handles floating point inputs by performing floor division and returning a float result."""
    assert AdvancedCalculator.integer_division(12.6, 3.0) == 4.0

@pytest.mark.generated
@pytest.mark.edge_case
def test_edge_case_zero_divisor():
    """Verify that dividing by zero returns the specific error message string instead of raising a ZeroDivisionError."""
    assert AdvancedCalculator.integer_division(10, 0) == "Cannot perform integer division by zero"

@pytest.mark.generated
@pytest.mark.edge_case
def test_edge_case_negative_numbers():
    """Verify floor division behavior with negative numbers (e.g., -11 // 4 is -2.75 which floors to -3)."""
    assert AdvancedCalculator.integer_division(-11, 4) == -3

@pytest.mark.generated
@pytest.mark.error_path
def test_error_path_type_error():
    """Verify that passing a non-numeric type (string) as an argument results in a TypeError."""
    with pytest.raises(TypeError):
        AdvancedCalculator.integer_division("20", 5)
