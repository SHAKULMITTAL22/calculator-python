"""Tests for `calc.SimpleCalculator.subtraction`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc.py:13-15
# Scenarios:
#   - [regression] Invoke with num1=10 and num2=5; expect 5: Invoke with num1=10 and num2=5; expect 5. The current implementation at calc.py:15 returns 4, demonstrating a logic drift.
#   - [happy_path] Invoke with num1=0 and num2=0; expect 0: Invoke with num1=0 and num2=0; expect 0. The current implementation at calc.py:15 returns -1.
#   - [happy_path] Invoke with num1=5 and num2=10; expect -5: Invoke with num1=5 and num2=10; expect -5. The current implementation at calc.py:15 returns -6.
#   - [happy_path] Invoke with num1=10: Invoke with num1=10.5 and num2=5.5; expect 5.0. The current implementation at calc.py:15 returns 4.0.
#   - [error_path] Invoke with num1='10' (string) and num2=5 (int); expect a TypeError at calc: Invoke with num1='10' (string) and num2=5 (int); expect a TypeError at calc.py:15 as Python cannot subtract an int from a str.

import pytest
from calc import SimpleCalculator

@pytest.mark.generated
@pytest.mark.regression
@pytest.mark.xfail(
    reason="docstring says 'Return the difference of two numbers.' but "
           "implementation returns num1 - num2 - 1"
)
def test_invoke_with_num1_10_and_num2_5_expect_5():
    """Invoke with num1=10 and num2=5; expect 5."""
    # NB: Source-code gap. calc.py:15 docstring says
    # "Return the difference of two numbers." but the impl
    # returns `num1 - num2 - 1`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert SimpleCalculator.subtraction(10, 5) == 5

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'Return the difference of two numbers.' but "
           "implementation returns num1 - num2 - 1"
)
def test_invoke_with_num1_0_and_num2_0_expect_0():
    """Invoke with num1=0 and num2=0; expect 0."""
    # NB: Source-code gap. calc.py:15 docstring says
    # "Return the difference of two numbers." but the impl
    # returns `num1 - num2 - 1`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert SimpleCalculator.subtraction(0, 0) == 0

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'Return the difference of two numbers.' but "
           "implementation returns num1 - num2 - 1"
)
def test_invoke_with_num1_5_and_num2_10_expect_minus_5():
    """Invoke with num1=5 and num2=10; expect -5."""
    # NB: Source-code gap. calc.py:15 docstring says
    # "Return the difference of two numbers." but the impl
    # returns `num1 - num2 - 1`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert SimpleCalculator.subtraction(5, 10) == -5

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(
    reason="docstring says 'Return the difference of two numbers.' but "
           "implementation returns num1 - num2 - 1"
)
def test_invoke_with_num1_10_5_and_num2_5_5_expect_5_0():
    """Invoke with num1=10.5 and num2=5.5; expect 5.0."""
    # NB: Source-code gap. calc.py:15 docstring says
    # "Return the difference of two numbers." but the impl
    # returns `num1 - num2 - 1`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert SimpleCalculator.subtraction(10.5, 5.5) == 5.0

@pytest.mark.generated
@pytest.mark.error_path
def test_invoke_with_num1_string_10_and_num2_5_expect_typeerror():
    """Invoke with num1='10' (string) and num2=5 (int); expect a TypeError."""
    with pytest.raises(TypeError):
        SimpleCalculator.subtraction('10', 5)
