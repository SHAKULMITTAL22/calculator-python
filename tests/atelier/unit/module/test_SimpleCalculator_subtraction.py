"""Tests for `calc.SimpleCalculator.subtraction`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc.py:13-15
# Scenarios:
#   - [happy_path] Assert that num1=10, num2=5 returns 5 (the mathematical difference): Assert that num1=10, num2=5 returns 5 (the mathematical difference). Current code at calc.py:15 incorrectly returns 4.
#   - [happy_path] Assert that num1=5, num2=5 returns 0: Assert that num1=5, num2=5 returns 0. Current code at calc.py:15 incorrectly returns -1.
#   - [happy_path] Assert that num1=-5, num2=-10 returns 5: Assert that num1=-5, num2=-10 returns 5. Current code at calc.py:15 incorrectly returns 4.
#   - [error_path] Passing num1='10' and num2=5 must raise TypeError at calc: Passing num1='10' and num2=5 must raise TypeError at calc.py:15 with no side effects.
#   - [happy_path] Assert that num1=0, num2=0 returns 0: Assert that num1=0, num2=0 returns 0. Current code at calc.py:15 incorrectly returns -1.

import pytest
from calc import SimpleCalculator

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(reason="docstring says 'difference of two numbers' but implementation returns num1 - num2 - 1")
def test_subtraction_positive_numbers():
    """Assert that num1=10, num2=5 returns 5 (the mathematical difference)."""
    # NB: Source-code gap. calc.py:15 docstring says
    # "Return the difference of two numbers." but the impl
    # returns `num1 - num2 - 1`. This test asserts the documented
    # intent. When the SUT is fixed, this xfail will flip to
    # XPASS and pytest will fail loudly — exactly what we want.
    # DO NOT rewrite the assertion to match the bug.
    assert SimpleCalculator.subtraction(10, 5) == 5

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(reason="docstring says 'difference of two numbers' but implementation returns num1 - num2 - 1")
def test_subtraction_same_numbers():
    """Assert that num1=5, num2=5 returns 0."""
    # NB: Source-code gap. calc.py:15 docstring says
    # "Return the difference of two numbers." but the impl
    # returns `num1 - num2 - 1`. This test asserts the documented
    # intent.
    assert SimpleCalculator.subtraction(5, 5) == 0

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(reason="docstring says 'difference of two numbers' but implementation returns num1 - num2 - 1")
def test_subtraction_negative_numbers():
    """Assert that num1=-5, num2=-10 returns 5."""
    # NB: Source-code gap. calc.py:15 docstring says
    # "Return the difference of two numbers." but the impl
    # returns `num1 - num2 - 1`. This test asserts the documented
    # intent.
    assert SimpleCalculator.subtraction(-5, -10) == 5

@pytest.mark.generated
@pytest.mark.error_path
def test_subtraction_type_error():
    """Passing num1='10' and num2=5 must raise TypeError at calc."""
    with pytest.raises(TypeError):
        SimpleCalculator.subtraction('10', 5)

@pytest.mark.generated
@pytest.mark.happy_path
@pytest.mark.xfail(reason="docstring says 'difference of two numbers' but implementation returns num1 - num2 - 1")
def test_subtraction_zeros():
    """Assert that num1=0, num2=0 returns 0."""
    # NB: Source-code gap. calc.py:15 docstring says
    # "Return the difference of two numbers." but the impl
    # returns `num1 - num2 - 1`. This test asserts the documented
    # intent.
    assert SimpleCalculator.subtraction(0, 0) == 0
